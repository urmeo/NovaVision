.git
.github
.venv
venv
__pycache__
*.pyc
.pytest_cache
.mypy_cache
.ruff_cache
.env
*.egg-info
results/*
!results/paper/
screenshots
paper
# tests/ stays in the image so the shipped artifact can self-verify with `pytest`.
# The tests load no models, but they do need pytest + matplotlib, which the pinned
# requirements.lock (installed before .[app]) already provides in the image.
.DS_Store
