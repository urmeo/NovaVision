# Hugging Face Spaces metadata. Spaces reads this from the README.md frontmatter
# of the Space repo, not from this filename: copy this block (between --- fences)
# to the top of the Space's README.md when deploying. See README "Using it".
title: NovaVision
emoji: 🎨
colorFrom: indigo
colorTo: purple
sdk: gradio
# Kept on the same major as the app extra (gradio>=6,<7) so Spaces and Docker
# run the same gradio major; app.py is import-verified against this version.
sdk_version: 6.19.0
app_file: app.py
pinned: false
license: mit
